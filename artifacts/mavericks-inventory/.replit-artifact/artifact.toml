kind = "web"
previewPath = "/"
title = "Mavericks Inventory"
version = "1.0.0"
id = "artifacts/mavericks-inventory"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 26222

[services.development]
run = "pnpm --filter @workspace/mavericks-inventory run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/mavericks-inventory", "run", "build" ]
publicDir = "artifacts/mavericks-inventory/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "26222"
BASE_PATH = "/"
